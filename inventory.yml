# https://docs.ansible.com/ansible/latest/plugins/inventory/yaml.html

# The "all" group contains all servers no matter what.
# Might as well configure it explicitly
all:
  hosts:

    # Fedora
    spacerig:
      expected_hostname: spacerig
      ansible_connection: local
      desktop_linux: true

    # Fedora
    houston:
      expected_hostname: houston
      ansible_connection: local
      desktop_linux: true

    # Fedora
    aurora:
      expected_hostname: aurora
      ansible_connection: local
      desktop_linux: true

