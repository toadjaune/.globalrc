#compdef sshrc=ssh mosh=ssh moshrc=ssh

# Completion definition for ssh wrappers
# NB : This could also be done with `compdef sshrc=ssh` in zshrc
#
# TODO: Mosh should have a specific completion (like in bash), but ssh completion is good enough for now

_ssh "$@"
